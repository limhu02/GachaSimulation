<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.sample3.mapper.BoardMapper">

	<select id="getBoards" resultType="com.springboot.sample3.model.FreeBoard">
	select id, title, content,to_char(write_date,'YYYY-MM-DD') write_date ,seq from free_board
	</select>
	<select id="getBoardDetail" parameterType="Integer" resultType="com.springboot.sample3.model.FreeBoard">
		select id, title, content,to_char(write_date,'YYYY-MM-DD') write_date ,seq from free_board where seq =#{id}
	</select>

</mapper>