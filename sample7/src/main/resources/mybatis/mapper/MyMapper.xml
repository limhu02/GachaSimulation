<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.sample7.mapper.MyMapper">
	<select id="getGuail" parameterType="Integer" resultType="com.springboot.sample7.model.Guail">
	select seqno, title, price, info, image_name from guail_tbl
	where seqno = #{itemId}
	</select>
	<select id="getGuailList" resultType="com.springboot.sample7.model.Guail">
	select seqno, title, price from guail_tbl
	</select>

	<insert id="putUser" parameterType="com.springboot.sample7.model.User">
	insert into user_info values(#{userId},#{password},#{userName},
	#{address},null,null,0,#{email},#{birthday},#{job})
	</insert>
	<select id="getUser" parameterType="com.springboot.sample7.model.User" 
					resultType="com.springboot.sample7.model.User">
	select user_id "userId",user_pwd "password", name "userName",
	addr "address", email, job, birth "birthday"
	from user_info 	where user_id=#{userId} and	user_pwd=#{password}
	</select>
	
	<select id="getUserById" parameterType="com.springboot.sample7.model.User" 
					resultType="com.springboot.sample7.model.User">
	select user_id "userId",user_pwd "password", name "userName",
	addr "address", email, job, birth "birthday"
	from user_info 	where user_id=#{userId}
	</select>
	
	<select id="getUserList" resultType="com.springboot.sample7.model.User">
	select user_id "userId",user_pwd "password", name "userName",
	addr "address", email, job, birth "birthday"
	from user_info
	
	</select>
</mapper>